name: hello-world1
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: my-step1
        run: |
          echo "${{ secrets.TESTKUNDE1 }}" | docker login -u "santosch" --password-stdin
#          docker image build -t santosch/reponeu:v${{ github.run_number }} .
#          docker push santosch/reponeu:v${{ github.run_number }}

